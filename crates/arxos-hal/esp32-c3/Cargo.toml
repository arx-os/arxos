[package]
name = "arxos-hal-esp32-c3"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "ESP32-C3 hardware support for the ArxOS runtime"

[lib]
crate-type = ["rlib"]

[dependencies]
arxos = { path = "../../arxos", optional = true }

