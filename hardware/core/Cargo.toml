[package]
name = "arxos-hardware-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
embedded-hal = "1.0"
heapless = "0.8"
thiserror = "1.0"
nb = "1.0"
chrono = { version = "0.4", features = ["serde"] }
