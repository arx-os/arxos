#![no_std]

//! ESP32-C3 hardware support for ArxOS.
//!
//! This crate exposes the board-specific primitives required to host the
//! `arxos` runtime on Espressif's ESP32-C3 microcontrollers.

pub fn init() {
    // Peripheral initialisation logic will be wired in alongside the driver
    // implementations as the HAL matures.
}
