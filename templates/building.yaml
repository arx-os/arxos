# Reference skeleton only. `arx init` generates building.yaml in code
# (UUID id + Ground Floor) and writes via validated save (Track I6).
# Do not ship empty id/floors as a pilot cold-start path.
schema_version: 1
building:
  id: "REPLACE_WITH_UUID"
  name: My Building
  path: /building
  description: A sample building managed with ArxOS
  version: "1.0.0"
  floors:
    - id: "REPLACE_WITH_UUID"
      name: Ground Floor
      level: 0
      wings: []
      equipment: []
      properties: {}
  coordinate_systems: []
equipment: []
