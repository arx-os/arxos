# Trunk 0.21+ config for ArxOS PWA (feature `web`).
# Features come from index.html: data-cargo-features="web"

[build]
target = "index.html"
release = true

[watch]
ignore = ["dist"]

[serve]
addresses = ["0.0.0.0"]
port = 8080
open = false

[clean]
dist = "dist"
cargo = false
