# Static landing only (Decision 9 — docs/adr-web-demotion.md).
# No Rust/WASM build. Serve the repo root files with any static server, e.g.:
#   python3 -m http.server 8080
#   npx serve .
# Trunk is no longer required for ArxOS web.
