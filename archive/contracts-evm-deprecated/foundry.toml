[profile.default]
src = "."
out = "out"
libs = ["lib"]
solc_version = "0.8.20"
optimizer = true
optimizer_runs = 200

# Base mainnet / sepolia chain ids for reference when broadcasting.
# forge script ... --rpc-url $BASE_RPC --broadcast
