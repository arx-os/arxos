# Archive

Historical material removed from the active product path. Nothing here is built or tested by default.

| Directory | Description |
|-----------|-------------|
| `contracts-evm-deprecated/` | Former EVM building registry (pure-fiat model: no blockchain settlement) |
